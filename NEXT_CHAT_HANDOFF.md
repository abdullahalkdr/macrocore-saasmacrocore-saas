# macrocore.io — تسليم للشات الجديد

انسخ هذا الملف كامل والصقه كأول رسالة بالشات الجديد. إذا عندك نسخة أصلية من السبيك الهندسي اللي كتبته أول مرة (فيه أسماء الحقول بالضبط)، الصقها بعده — القسم الأخير بهذا الملف "الوحدات الباقية" هو ملخص من الذاكرة مو نسخة حرفية.

---

## 1. عن المشروع

**macrocore.io** — نظام SaaS كويتي لإدارة أكشاك/عربات الأكل (kiosk/food-truck management).

**الموقع على الجهاز:**
- المشروع الكامل: `C:\Users\USER\Desktop\macrocore\macrocore-saas`
- الباك-إند: `C:\Users\USER\Desktop\macrocore\macrocore-saas\backend`
- الفرونت-إند: `C:\Users\USER\Desktop\macrocore\macrocore-saas\frontend`

**التقنيات:**
- الباك-إند: Node.js + Express + TypeScript. **بدون ORM** — SQL خام عبر مكتبة `pg` مباشرة (كل الكويريز مكتوبة يدوي بالكنترولرز). قاعدة البيانات PostgreSQL مستضافة على Railway.
- الفرونت-إند: React 18 + TypeScript + Zustand (state management، مع `persist` middleware) + react-router-dom v6 + Vite.
- تصميم UI مطابق لنظام مرجعي اسمه "CornLab" (كشك ذرة/بطاطا كويتي) — ألوان كهرمانية/برتقالية (#F5A623 أساسي)، sidebar غامق، محتوى أبيض، RTL كامل. الخط Tajawal.
- **ثنائي اللغة بالكامل**: عربي (افتراضي) / إنجليزي، عبر ملف `frontend/src/i18n.ts` (قاموس `en`/`ar` مطابق بالبنية، hook `useT()`)، مع `useLangStore` (zustand) يتحكم بـ `dir`/`lang` على `<html>`.
- **صلاحيات**: أدوار `admin` / `manager` / `employee`. الباك-إند يفرضها بـ middleware (`requireRole`)، والفرونت بمكوّن `RequireRole` (يعرض رسالة "غير مصرح" بدل ريدايركت).

## 2. البنية التقنية (مهم تلتزم بها بالضبط)

### الباك-إند
- كل جدول: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `company_id UUID NOT NULL REFERENCES companies(id)` (multi-tenant — كل صف مربوط بشركة).
- الـ auth: JWT، `req.auth = { userId, companyId, role }` بعد `requireAuth` middleware.
- كل موديول له: `controllers/<name>.controller.ts` + `routes/<name>.routes.ts`، مسجّلة بـ `src/app.ts`.
- **الميقريشنز إضافية فقط (additive-only)**: ما نقدر نوصل قاعدة بيانات Railway الحقيقية مباشرة من بيئة العمل، فكل تعديل schema يتسلّم كملف SQL منفصل بـ `backend/docs/MIGRATION_00X_<name>.sql` يستخدم `ADD COLUMN IF NOT EXISTS` / `CREATE TABLE IF NOT EXISTS`، والمستخدم يشغّله يدوي على قاعدته الحقيقية. **بالتوازي** يتحدّث `backend/docs/DATABASE_SCHEMA.sql` (الملف الرئيسي) بنفس الإضافات عشان أي تنصيب جديد يجيه كامل من البداية.
- الميقريشنز الموجودة لين الحين: `MIGRATION_002_priority1.sql` (تكلفة المنتجات + قنوات البيع)، `MIGRATION_003_product_sizes.sql` (أحجام المنتج)، `MIGRATION_004_attendance_hr.sql` (الحضور + الموارد البشرية).
- **التحقق (verification)**: نستخدم `pg-mem` (محرك SQL متوافق مع Postgres يشتغل بالذاكرة) لفحص كل منطق جديد فعلياً بدون شبكة. فيه نسخة شغالة بـ `~/build/macrocore-saas/backend/` (بمجلد bash المؤقت، مب بمجلد المستخدم) فيها `pg-mem` مثبتة، تُستخدم بكتابة `*_smoke.js` سكربتات تشغّل السيرفر كامل (`dist/app.js`) فوق `pg-mem` بدل `pg` الحقيقية وترسل HTTP requests حقيقية وتتحقق من النتائج.
  - **ملاحظة مهمة**: `pg-mem` ما يدعم `DECIMAL(p,s)` مع default رقمي مباشر (لازم `NUMERIC` بدلها بنسخة الاختبار بس)، وما يدعم `IS NOT DISTINCT FROM` ولا `= ANY($1::type[])` array params، ولا `AGE()`/`EXTRACT` functions. لما تصادف هالقيود، غيّر الكود الحقيقي لصيغة مكافئة تشتغل بالاثنين (مثال: `(a = $1 OR (a IS NULL AND $1 IS NULL))` بدل `IS NOT DISTINCT FROM`)، أو احسب بـ JavaScript بدل SQL إذا كانت دالة مش مدعومة.

### الفرونت-إند
- كل صفحة CRUD: نمط موحّد — `PageHeader` + `section-title-row` (عدّاد + زر "+جديد") + `data-table` (بأعمدة رقمية تاخذ class `.num` بـ `<th>` **و** `<td>` عشان تصير LTR دايم) + `Modal` (مكوّن مشترك) للإنشاء/التعديل.
- كل نص بالواجهة لازم يمر عبر `useT()` — ما نكتب نصوص إنجليزي/عربي مباشرة بالـ JSX.
- الملفات المرجعية المشتركة: `components/Icon.tsx` (أيقونات SVG جاهزة)، `components/Modal.tsx`، `components/Tag.tsx`، `components/PageHeader.tsx`، `components/Avatar.tsx`، `components/StatCard.tsx`.
- الملفات المرفوعة (صور، شهادات، مرفقات) تُخزّن base64 داخل الـ JSON مباشرة (ما فيه object storage حالياً) — نفس نمط `waste_records.image_base64` القديم.

## 3. الحالة الحالية (كل شي شغال ومُتحقق منه)

منجز بالكامل ومُتحقق (باك-إند + فرونت-إند + pg-mem + `tsc` نظيف):
1. الأساسيات: auth، شركات، مستخدمين، منتجات، مواد خام، موظفين، مواقع، شفتات/POS، مبيعات، تقارير، مصروفات، هالك، رواتب، تذاكر دعم، مزامنة أوفلاين (sales فقط)، admin panel.
2. **إعادة تصميم كاملة** للواجهة تطابق نظام CornLab المرجعي + ثنائية لغة كاملة + صلاحيات صفحات.
3. **تكلفة المنتجات**: مواد خام بأسماء ثنائية اللغة، وصفات (`product_ingredients`)، تكلفة ثابتة شهرية (`companies.fixed_cost_items`)، تقدير عدد طلبات شهري (تلقائي أو يدوي)، `GET /products/:id/cost` يحسب raw_cost + overhead + هامش ربح.
4. **قنوات البيع والعمولة**: cash/knet/jahez/vthru، عمولة افتراضية بالشركة تُقفل على الموظف وتُفتح للمدير، والتقارير تحسب صافي الإيراد بعد العمولة.
5. **أحجام المنتج (S/M/L)**: `product_sizes` + `product_size_ingredients`، كل حجم له سعره ووصفته، الشفت يوزّع المخزون لكل حجم لحاله (`shift_assignments.product_size_id`)، والبيع يسحب من الحجم الصحيح.
6. **الحضور والموارد البشرية**: `attendance_records` (حضور/انصراف + حساب تأخير بالدقيقة تلقائي + خصم = الراتب ÷ (أيام العمل × دقائق الشفت) × دقائق التأخير، معتمد على إعدادات شركة `official_shift_start_time`/`grace_period_minutes`/`working_days_per_month`/`standard_shift_minutes`)، `leave_requests` (إجازة سنوية/مرضية/استئذان + مرفق + موافقة/رفض + تقويم شهري ملون `GET /leave-requests/calendar`)، وملف موظف موسّع (صورة، رقم مدني، تاريخ ميلاد بعمر محسوب بـ JS، وزن، خبرة سابقة، شهادات متعددة بمرفقات، مع `PATCH /employees/:id` للتعديل).

**قاعدة صارمة يلتزم فيها الشات الجديد**: لا تلمس أي شي من هذا كله. كل شي فوق شغال ومُختبر. أي تعديل عليه يكون فقط إذا احتاجته الوحدة الجديدة (مثال: إضافة عمود بميقريشن إضافية) ويكون *إضافي* مو استبدال.

## 4. الوحدات الباقية (المطلوبة بهالشات الجديد)

⚠️ التفاصيل هنا ملخّص عام من الذاكرة، مو نص حرفي من السبيك الأصلي. إذا عندك النص الأصلي الصقه.

### أ) مخزون FIFO / تاريخ انتهاء الصلاحية
فكرة عامة: بدل ما `raw_materials` يكون رقم واحد "كمية متوفرة"، نحتاج **دفعات شراء (batches)** — كل مرة تشتري مادة خام (كمية + تاريخ شراء + تاريخ انتهاء صلاحية اختياري)، يتسجل batch جديد. لما يُستهلك مخزون (بيع منتج، هالك، تحضير) يُخصم من أقدم batch أول (FIFO). محتاج:
- جدول جديد `raw_material_batches` (id, company_id, raw_material_id, purchase_date, expiry_date, qty_purchased, qty_remaining, purchase_price).
- منطق استهلاك FIFO عند تسجيل بيع/استخدام (يمشي على الدفعات بالترتيب لين يغطي الكمية المطلوبة).
- تنبيه/تقرير للمواد القريبة من الانتهاء.
- احتمال يأثر على حساب التكلفة (`costing.ts`) — التكلفة الحقيقية تصير من سعر آخر دفعة استُهلكت، مو من `purchase_price` الثابت بجدول `raw_materials`.

### ب) فصل المواقع كشك/مستودع
`locations` الحالي عنده name/address/area بس. المطلوب: نوع للموقع (`type`: kiosk أو warehouse)، ومخزون منفصل لكل موقع (يعني المخزون مب على مستوى الشركة كلها، لأنه يتوزع بين المستودع والأكشاك)، وعمليات "تحويل مخزون" بين مستودع وكشك.

### ج) رواتب متقدمة
`payroll` الحالي بسيط (راتب أساسي + بدل حضور + خصومات، شهري). المطلوب توسيعه: أجر بالساعة كخيار بديل عن الراتب الشهري الثابت، ربط تلقائي مع `attendance_records.deduction_amount` (خصم التأخير يدخل بحساب الراتب تلقائي عند التوليد الشهري)، وبنود تعديل يدوية (bonuses/deductions إضافية بخط تفصيلي مو رقم واحد).

### د) موديول المستندات (أولوية أقل / اختياري)
مستندات الشركة (تراخيص، عقود، شهادات...) — رفع، تخزين (نفس نمط base64)، تاريخ انتهاء صلاحية وتنبيه، تصنيف.

## 5. أسلوب العمل المتوقع

- اسأل بس إذا كان القرار يحتاج توجيه المستخدم فعلاً (شكل schema، أولوية بين الوحدات). القرارات التقنية الصغيرة سوّها وبلّغ بس.
- كل ميزة جديدة: schema إضافي → باك-إند (controller/routes) → تحقق pg-mem → فرونت-إند → `tsc -b` نظيف على الاثنين → تقرير مختصر للمستخدم بالكويتي، يوضح شنو انبنى وشنو أُجّل وليش.
- خلك مباشر وعملي، بدون فذلكة.
